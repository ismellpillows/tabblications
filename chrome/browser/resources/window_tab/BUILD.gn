import("//chrome/common/features.gni")
import("//tools/grit/grit_rule.gni")
import("//ui/webui/resources/tools/generate_grd.gni")

generate_grd("build_grd") {
  grd_prefix= "window_tab"
  out_grd = "$target_gen_dir/resources.grd"
  input_files = [ "window_tab.html" ]
  input_files_base_dir = rebase_path(".", "//")
}

# Create the pak, header, and resource map files.
grit("resources") {
  defines = chrome_grit_defines

  # This arguments is needed since the grd is generated at build time.
  enable_input_discovery_for_gn_analyze = false
  source = "$target_gen_dir/resources.grd"
  deps = [ ":build_grd" ]
  outputs = [
    "grit/window_tab_resources.h",
    "grit/window_tab_resources_map.cc",
    "grit/window_tab_resources_map.h",
    "window_tab_resources.pak",
  ]
  output_dir = "$root_gen_dir/chrome"
}
